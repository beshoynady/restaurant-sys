# Restaurant ERP SaaS

**Required reading before working on this codebase:** [docs/PROJECT_VISION_ar.md](docs/PROJECT_VISION_ar.md)

That document is the architectural source of truth — SaaS multi-tenancy model (Platform → Brand → Branch → Department → Employee), Modular ERP / Feature Toggle design, Settings Driven Architecture, the Security Pipeline every request must pass through, Order lifecycle, Kitchen Display System, Inventory/Recipe/Production flow, Accounting, Multi-Tenancy scoping rules, and Audit requirements. Any new module or architectural decision must align with it.

**Language policy:** the project itself — code, code comments, and all documentation — is English only. (Chat with the assistant may happen in Arabic; that doesn't apply to anything committed to the repo.) `docs/PROJECT_VISION_ar.md` is a deliberate one-off exception (the owner's original vision doc, kept in Arabic); every other doc, including all module docs, must be English, professional, technical, and simple — written for a programmer who needs to read and modify the code, not a business audience.

## Repo layout

- `server/` — Node.js/Express/MongoDB backend (see `server/modules/<domain>/<entity>/` — each entity follows `model.js` + `service.js` + `controller.js` + `router.js` + `validation.js`, built on `server/utils/BaseService.js` / `server/utils/BaseController.js`).
- `app/` — frontend.
- `docs/` — project documentation. `docs/PROJECT_VISION_ar.md` is the architectural source of truth (Arabic, exception — see language policy above). Server-level technical docs and one doc per module are planned (English) — see "Documentation system" below.

## Documentation system

Two levels, English, technical and concise, aimed at a programmer picking up the code:

1. **Server-level doc** — [server/BACKEND_FOUNDATION.md](server/BACKEND_FOUNDATION.md). Covers the foundation layer: what changed in the 2026-07-11 review, why, how the request pipeline/`BaseService`/`BaseController`/RBAC/feature-toggle/multi-tenancy work now, and the mandatory checklist for building any new module on top of it. Read this before touching `server/`.
2. **[server/ARCHITECTURE_REVIEW.md](server/ARCHITECTURE_REVIEW.md)** — structural + business architecture review: naming issues, domain-boundary problems, duplicate/legacy trees, module classification, entity state machines, business rules, ownership/source-of-truth, event catalog, ERP gap analysis, and readiness ratings for every module. Analysis only — frozen as the architecture reference, nothing in it has been implemented.
3. **[server/IMPLEMENTATION_PLAN.md](server/IMPLEMENTATION_PLAN.md)** — the execution roadmap derived from the Architecture Review: foundation tasks, the full refactoring backlog (title/priority/risk/affected files per item), module build order with rationale, settings-implementation scheduling, phase-by-phase plan (7 phases), risks, and success criteria. This is what to follow when implementation actually starts — check here before picking up any task.
4. **[server/DATABASE_MODELS_REVIEW.md](server/DATABASE_MODELS_REVIEW.md)** — models-only architecture review (2026-07-11): every Mongoose schema audited as one connected ERP database (not module-by-module). Covers cross-cutting defect patterns (global-unique fields that break multi-tenancy, the broken `JournalEntry`↔`JournalLine` relationship, missing recipe/product cost fields, inconsistent soft-delete/actor-reference conventions), a per-model 9-point audit, full sales/inventory/accounting chain consistency check, and a scored final ERP assessment. Analysis only — nothing in it has been implemented; check here before touching any model file to see if it's already a known issue.
5. **[server/DATABASE_ARCHITECTURE_REDESIGN.md](server/DATABASE_ARCHITECTURE_REDESIGN.md)** — the architectural redesign that follows the models review: multi-tenancy uniqueness scoping rules (three identifier classes, not "prepend brandId everywhere"), the corrected double-entry `JournalEntry`/`JournalLine` design, the three-tier costing strategy (current cost basis / cached recipe cost / immutable sale-time snapshot, plus a new `StockLayer` and `UnitOfMeasure` model), the tamper-evident append-only `AuditLog` redesign, the multilingual architecture evaluation (kept Map<String,String>, repaired rather than replaced), the full Data Ownership / Uniqueness / Cost Flow / Derived Data / Accounting Metadata matrices, a 30-workflow business validation pass, and the final Critical/Important/Planned Feature classification of every remaining gap. This is the complete, approved architecture — analysis and design only, no model files changed.
6. **[server/DATABASE_IMPLEMENTATION_PLAN.md](server/DATABASE_IMPLEMENTATION_PLAN.md)** — the execution plan translating every Critical/Important item from the redesign into engineering tasks (`DB-001`...), grouped into epics, phased (Phase 0 Critical blocks production, Phase 1 Important is fast-follow, Phase 2 Planned Features is backlog only), each task with files/dependencies/migration requirements/risk/acceptance criteria. This is a living execution record — update task status here as work lands. Start here when picking up any database-architecture implementation work. Phase 0 is complete as of 2026-07-12 (all 20 tasks, verified via a Jest integration suite — see `server/tests/integration/`).
7. **[server/REPOSITORY_PATTERN_MIGRATION_PLAN.md](server/REPOSITORY_PATTERN_MIGRATION_PLAN.md)** — the 2026-07-12 architecture standard mandating a Repository Pattern (see `BACKEND_FOUNDATION.md` §4.3) for every module: a service class extends a `<entity>.repository.js` class (both files flat at the domain root — file-suffix separation, not folder separation). Live on considerably more modules than "pilot only" now suggests — check `DOMAIN_ENGINE_ARCHITECTURE_MIGRATION_PLAN.md` §3 for the current inventory before assuming a module hasn't been migrated.
8. **[server/DOMAIN_ENGINE_ARCHITECTURE_MIGRATION_PLAN.md](server/DOMAIN_ENGINE_ARCHITECTURE_MIGRATION_PLAN.md)** — a separate, broader effort from #7: real `repositories/`/`services/`/`engines/` **subfolders** per domain (not just file-suffix separation), plus a strict `Controller → Service → Engine → Repository` dependency rule. Piloted on `sales/order` only (2026-07-15) — verified, documented, not yet rolled out further. Read this before assuming any other module should be restructured this way, and before extracting a new "Engine" class anywhere (it documents which extractions were judged premature on the pilot and why).
9. **Per-module doc** (one file per entity, e.g. `modules/hr/employee/EMPLOYEE.module.md` — already-existing examples to follow as the template): business purpose, schema field/relationship reference, endpoints with required permissions, non-CRUD business logic, related settings.
10. **[server/ERP_DEVELOPMENT_STANDARD.md](server/ERP_DEVELOPMENT_STANDARD.md)** — the mandatory, codebase-wide development standard (2026-07-16), extracted directly from the certified Financial Domain (see `server/FINANCIAL_DOMAIN_CERTIFICATION.md`) after it became the first fully-built, fully-tested, fully-documented domain in this ERP. Defines the required pattern for every layer (Model/Repository/Service/Controller/Validation/Route), API/reporting/dashboard conventions, testing requirements, and a per-module Definition of Done. **Read this before starting any new module** — including Menu/Kitchen/Production — and use `server/templates/module-template/` as the literal starting point (copy it, rename the placeholders) rather than building a module from a blank file. Confirms (not overrides) that the per-module doc convention in item 9 above stays a single `<ENTITY>.module.md` file, 14 sections — a explicit fork to a multi-file-per-module doc structure was proposed and declined when this standard was written.
11. **[server/PREPARATION_INVENTORY_ORDER_FLOW_AUDIT.md](server/PREPARATION_INVENTORY_ORDER_FLOW_AUDIT.md)** — cross-module audit (2026-07-17) of `sales/order` / `sales/order-settings` / `inventory/inventory-settings` / `inventory/recipe-consumption` / `preparation/preparation-ticket` / `preparation/preparation-section`, checked against the Business Decision Matrix (Chapter 21, supplied inline by the owner — not yet a committed repo file). Three fixes already implemented: the `inventoryDeductionTrigger` setting (closes §21.5); removal of the dead, conflicting `OrderSettings.preventNegativeStockOrders` field (`InventorySettings.allowNegativeStock` is now sole source of truth); and preparation-phase-aware item cancellation in `order.service.js#cancelItem` (closes §21.4's phase-awareness gap, derived from the live `PreparationTicket.preparationStatus` signal since `OrderItem.status` is confirmed dead too). Open findings, recommendations pending approval: `OrderSettings` is still 14/16-fields dead, §21.3 (Order Acceptance Decision) unimplemented, payment-status gating on cancellation is blocked on a prerequisite gap (`Order.paymentStatus`/`Invoice.status` both dead, no live payment signal exists anywhere), and 4 dead fields on `PreparationSectionConfig`. Check here before touching any of the six listed modules.

12. **[server/PAYMENT_LIFECYCLE_AUDIT.md](server/PAYMENT_LIFECYCLE_AUDIT.md)** — architectural audit (2026-07-18) of the sales-side payment lifecycle, triggered by item 11's discovery that `Order.paymentStatus`/`Invoice.status` are both dead. Finding: the sales-side payment chain is unimplemented beyond GL posting at invoice creation — no code records a payment being collected, `CashTransaction` ("single source of truth for ALL money transactions") has zero production creators, and `CashierShift` reconciliation is real as a mechanism but starved of real data as a result. The Purchasing/AP side already has a working precedent (`PurchaseInvoice.recordPayment()`, `PurchaseReturn.recordRefund()`) with no Sales/AR equivalent — the recommended template for a future fix. Also found: `payment-channel`/`payment-provider` routers are not just unmounted but import-broken (would crash the app if mounted as-is), and zero external payment gateway integration exists anywhere. **Status: analysis only, nothing implemented.**

13. **[server/ENTERPRISE_PAYMENT_ARCHITECTURE_DESIGN.md](server/ENTERPRISE_PAYMENT_ARCHITECTURE_DESIGN.md)** — full target architecture (2026-07-18) for the payment domain, built on item 12's findings. **Most consequential finding: `Invoice.create()`'s existing journal posting is accounting-incorrect** — it debits a synthetic "cash" line for the full invoice total regardless of whether any payment was ever collected, meaning every invoice posted to date has overstated cash / understated Accounts Receivable. Designs a `Payment`/`Refund` aggregate pair mirroring the platform's own working Purchasing/AP precedent (`PurchaseInvoice.recordPayment()`/`PurchaseReturn.recordRefund()`), a corrected AR-based invoice posting, new `JournalLine.sourceType` values, new domain events, and a phased roadmap with Phase 0 (the AR correctness fix) called out as the single highest-priority item regardless of what else is approved. Also confirms several adjacent concepts are fully absent from the codebase (Wallet/Gift Card/Store Credit, Tips, real multi-currency conversion, Order channel types beyond DINE_IN/DELIVERY/TAKEAWAY/INTERNAL, Void/Comp as distinct from Cancel) and explicitly defers multi-currency to a separate, already-on-record platform-wide initiative rather than retrofitting it here. **Status: specification only, nothing implemented or approved.**

14. **[server/ADR-001-SALES-PAYMENT-ARCHITECTURE.md](server/ADR-001-SALES-PAYMENT-ARCHITECTURE.md)** — the official Architecture Decision Record for item 13, re-verified independently from primary source (not carried over blindly). **Corrects item 13**: `AccountingSettings.controlAccounts.accountsReceivable` already exists and is schema-required (`accounting-setting.model.js:40-44`) — item 13 incorrectly implied a new account needed provisioning. Confirms via direct comparison that `purchasing/purchase-invoice` already does this correctly (credits `accountsPayable` at invoice time, settles via `recordPayment()`) — the same bug pattern exists nowhere on the AP side, strong evidence this was a defect, not a design choice. Also recommends, under strict Single-Source-of-Truth review, that `Order.paymentStatus` be **removed** rather than wired (Invoice is the fiscal document; duplicating payment state on Order re-introduces the exact problem this ADR fixes — not yet actioned, out of Phase 0's scope). Selects a `Payment`/`Refund` aggregate design (Option B) over a patch-only fix (Option A, incomplete) or a full Customer sub-ledger (Option C, deferred — no confirmed need for house accounts/multi-invoice settlement yet). **Status: Phase 0 and Phase 1 approved and implemented (2026-07-19); Phase 1 refactored onto MongoDB transactions (2026-07-20).** Phase 0: `invoice.service.js` (converted from `.ts`, see below) now debits Accounts Receivable, not Cash. Phase 1: a new `sales/payment` module (`POST /sales/payments`) records real tenders against an invoice, atomically draws down `Invoice.balanceDue`/`amountPaid`, creates the platform's first real `CashTransaction` per tender, and posts a `SALES_PAYMENT_RECEIPT` journal entry — closing `PAYMENT_LIFECYCLE_AUDIT.md`'s core finding. An independent post-implementation audit found and fixed 3 bugs same-day (CashTransaction.cashRegister never propagated — broke shift reconciliation; split-tender payments collapsed onto one GL account; an unscoped cross-tenant CashRegister lookup), then escalated 2 more (no transaction around the multi-document write; an idempotency-key race under true concurrency) as an Architecture Decision Proposal. **Platform decision (2026-07-20): MongoDB Transactions are now the standard mechanism for every multi-document financial write in this ERP** — `BaseRepository#withTransaction()` (used by 17+ modules) now delegates to the MongoDB driver's own native `session.withTransaction()` (the officially documented retry pattern) rather than a hand-rolled version; `PaymentService.recordPayment()` runs its entire body (balance decrement, Payment create, every CashTransaction, the JournalEntry post) inside one transaction, closing both remaining findings. `journalEntryService`/`accountingSettingService`/`journalLineRepository`/`cashierShiftSettingsService` gained an optional externally-owned `session` parameter (backward compatible) so Payment's transaction can include the journal posting instead of opening a second, unrelated one. Verified: 0 lint/syntax errors, `sales-payment-recording.test.ts` expanded 5→10 tests (added concurrent-duplicate-idempotency-key and two rollback tests), full regression sweep of the entire suite (75 suites/327 tests, not just payment-adjacent) — zero failures. **Phases 3-4 (further cash-transaction/reporting integration, external providers) remain unapproved.** **Phase 2 (Refund) architecture decided 2026-07-20, implemented 2026-07-21** — see item 15, `server/ENTERPRISE_REFUND_ARCHITECTURE_DESIGN.md`, for the full design and its adversarial review. Summary: two aggregates, not one — `SalesReturn` (Sales context) owns the financial Step A (`SALES_RETURN`)/Step B (`SALES_REFUND`) reversal, `PreparationReturn` (Preparation context, a real but previously-overlooked dormant aggregate with its own `WASTE`/`RETURN_TO_STOCK`/`RESELLABLE` per-item decision field) owns kitchen/inventory disposition, referenced by ID exactly as `PreparationTicket` already references `Order` — not merged into one document. Approval is driven by settings-based `decisionBy`/threshold policy, not RBAC permissions: `SalesReturnSettings` for the financial side; the kitchen-disposition side lives in `PreparationSettings.return.*` (item 16 below) — `PreparationReturnSettings` itself was fully removed on 2026-07-21, superseding what this paragraph originally said. **Implementation**: `SalesReturnService` replaced its 16-line CRUD shell with real `requestRefund`/`approve`/`reject`/`settleRefund` logic (transactional, idempotent, split-tender-safe); `PreparationReturn` gained a `finalize()` action wiring `items[].decision` to real inventory movement for the first time (`RETURN_TO_STOCK`/`RESELLABLE` → `ReturnIssuance`; `WASTE` → restore-then-immediately-waste through `WasteRecord{CustomerReturnWaste}`, since the original sale already deducted the ingredients once); `warehouseDocumentService.postDocument()`/`wasteRecordService.approve()` retrofitted with the same external-session pattern Payment Phase 1 established; `order.service.js#cancelItem` gained a paid-order guard. Two pre-existing defects found and fixed along the way (not scope creep, both live bugs in already-shipped code): Payment's and SalesReturn's idempotency-key unique index used `sparse: true` on a compound index, which does not actually skip a document when the *other* indexed fields are present (a real MongoDB semantics gap, not a hypothetical) — fixed on both models via `partialFilterExpression`; `postDocument()`'s session retrofit didn't thread the session into its own opening read, making same-transaction documents invisible to it — fixed. Full platform regression: 80 suites/354 tests, 0 failures. See `ADR-001-SALES-PAYMENT-ARCHITECTURE.md`'s own Phase 2 implementation record for complete detail.

15. **[server/ENTERPRISE_REFUND_ARCHITECTURE_DESIGN.md](server/ENTERPRISE_REFUND_ARCHITECTURE_DESIGN.md)** — ADR-001 Phase 2's design document (2026-07-20), 24 sections (business/technical/accounting/inventory analysis, DDD, MongoDB transaction design, state machines, sequence diagrams, risk/gap analysis, alternatives), plus an appended "Architecture Review" section (adversarial self-review, performed before any ADR content was finalized) that found and corrected real errors in the original pass — most significantly, an entire dormant `PreparationReturn` aggregate and two dormant settings models (`SalesReturnSettings`, `PreparationReturnSettings`) that the first pass missed, and confirmed `warehouseDocumentService.postDocument()` is not merely unverified but **actively incompatible** with participating in an external MongoDB transaction (it opens its own independent one internally). **Status: architecture decided and recorded in ADR-001 (2026-07-20) after a follow-up cross-domain dependency sweep (Coupons/Wallet/Customer-Credit/generic-Approval-engine/Delivery — none exist) found no further gaps. Implementation not started.**

16. **Preparation bounded-context configuration** — `server/PREPARATION_DOMAIN_ARCHITECTURE_REVIEW.md` (2026-07-20, implemented same day: unified settings, shared `TransitionGuard`, `FryerOilLog` deleted per an explicit owner override), `server/PREPARATION_SETTINGS_ENTERPRISE_ARCHITECTURE_REVIEW.md` (found `PreparationSettings`'s own `notifications`/`escalation` fields duplicated the already-real `NotificationSettings.preparationSection`), `server/PREPARATION_CONFIGURATION_PLATFORM_ENTERPRISE_DESIGN.md` (the platform-wide Single-Source-of-Truth matrix that recommended trimming `PreparationSettings` down to business policy only), and `server/PLATFORM_DEVICE_ROUTING_ARCHITECTURE_VALIDATION.md` (design-only; rejected that same document's own `PreparationDevice`/`PreparationRoutingService` proposal, relocating the concept to a not-yet-built `modules/platform/` namespace as `OperationalDevice`/`RoutingEngine` — **not implemented**, awaiting approval). **Status (2026-07-21): `PreparationSettings` is now the ONLY configuration aggregate for the Preparation bounded context** — `PreparationTicketSettings` and `PreparationReturnSettings` have been fully removed (no model/service/controller/router/validation exists for either; their MongoDB collections are untouched and folded forward by a one-time lazy migration in `preparation-settings.service.js#migrateLegacySettings()`, which reads the raw collections via the MongoDB driver rather than importing a model, since none remains). `PreparationSettings` itself is scoped to genuine Preparation business policy only (`ticket`, `return`, `queue`, `sla`) — `notifications`/`escalation`/`printing`/`display`/`routing`/`safety`/`quality`/`shift`/`inventoryBehavior`/`waste.defaultWasteCategory` were all removed from the schema (each either duplicated a better-owned platform settings module or had no real consumer). `onboarding-engine.service.js` now seeds `PreparationSettings` (not the deleted `PreparationTicketSettings`) for every new brand. Full regression: all Preparation + onboarding suites pass.

17. **`server/PREPARATION_PLATFORM_ENTERPRISE_IMPLEMENTATION_REVIEW.md`** (2026-07-21) — post-consolidation implementation review: configuration layer confirmed solid; found two real operational gaps (zero query-supporting indexes anywhere in Preparation; `PreparationSectionConfig` had no `CostCenter` link, blocking department-level cost reporting) plus one "configured but inert" control (`PreparationSettings.return.decisionBy` migrated forward but never enforced). **`server/PREPARATION_SECTION_ENTERPRISE_WORK_CENTER_REVIEW.md`** (2026-07-21) — a deeper, DDD-grounded review of `PreparationSectionConfig` specifically, explicitly pushing back on a broader request to add Shift/HR, analytics, and HACCP/food-safety fields directly to the aggregate (all three rejected on SSOT/derived-data/no-existing-infrastructure grounds — see the doc's own §4/§12/§15) and narrowing the safe scope to three additive fields. **Both implemented 2026-07-21** under Enterprise Engineering Governance mode (ordinary, additive, non-breaking engineering — no separate approval gate required): `PreparationTicketSchema` and `PreparationReturnSchema` each gained one compound index matching their actual, verified query shape (`getKitchenQueue()`'s filter/sort for tickets; the generic brand/branch/status list sorted by `createdAt` for returns); `PreparationSectionConfig` gained `costCenter` (ref `CostCenter`, nullable), `operationalStatus` (`OPEN`/`TEMPORARILY_CLOSED`/`MAINTENANCE`, default `OPEN` — additive alongside, not replacing, the existing `isActive`), and `fallbackSection` (self-ref, same shape as `parentDepartment`). **Deliberately NOT implemented**: `decisionBy` enforcement (would change who can legally finalize a return for any brand with it already configured — a real business-behavior change, held for an explicit decision) and everything the Work Center review named out of scope (Shift/HR duplication, stored analytics/utilization fields, HACCP/quality-checklist data — see that doc for the reasoning). New test: `server/tests/integration/preparation-section-work-center-fields.test.ts` (4 tests, schema-level only — no existing service reads the new fields yet). Full regression: 0 new failures.

18. **[server/ENTERPRISE_PAYMENT_PLATFORM_ENGINEERING_REVIEW.md](server/ENTERPRISE_PAYMENT_PLATFORM_ENGINEERING_REVIEW.md)** (2026-07-21) — full audit of `modules/payments/*` before any redesign: of four submodules, only `payment-method` was actually live (mounted at `/finance/payment-methods`); `payment-channel` was fully built but never mounted; `payment-provider` was a bare stub with a broken router import that would have crashed the app if mounted; `payment-settings` was empty placeholders. Confirmed no encryption-at-rest capability existed anywhere in the codebase (every prior `crypto` usage was one-way hashing) and no webhook infrastructure existed at all. Proposed a `PaymentGateway` (adapter/strategy catalog) → `PaymentProvider` (a brand's configured use of a gateway) → `MerchantAccount` (the actual credentialed account, N per provider) hierarchy, mirroring the `SalesReturn`/`PreparationReturn` two-aggregate cross-context-reference precedent. **Phase 0 + Phase 1 (Gateway/Provider/MerchantAccount core) implemented same day, 2026-07-21.** Retired `payment-channel`/`payment-provider`/`payment-settings` outright (zero production data in any of the three, confirmed before deleting) rather than migrating dead stubs; `PaymentMethod.type` enum and `CashTransaction.paymentChannel` (renamed `paymentProvider`) repointed at the new `PaymentProvider` model. New: `utils/secretEncryption.js` (AES-256-GCM, the platform's first reversible-encryption utility — every prior `crypto` use in this codebase was one-way HMAC/SHA-256 hashing); `PaymentGateway` (capabilities + dynamic `credentialSchema` — an admin can onboard a brand-new gateway from data alone, no code change); `PaymentProvider` (Global/Brand/Branch scoping, priority-ordered fallback, channel restriction — `resolveCandidates()`); `MerchantAccount` (encrypted secret credentials, branch/register/channel restriction lists, `resolveAccount()`, a `toSafeJSON()` view that unconditionally masks every secret regardless of the caller's RBAC permission — a stricter-than-RBAC rule, not a permission check). New RBAC resources: `PaymentProviders`, `PaymentProviderCredentials` (deliberately separate from `PaymentProviders` — managing which providers exist is not the same trust level as touching live merchant secrets), `PaymentProviderMappings`, `PaymentGatewayTransactions`, `PaymentWebhookEvents` (the last three reserved for later phases). Verified: full app boot via `tsx server.js` confirmed the entire router chain resolves cleanly; new `tests/integration/enterprise-payment-platform.test.ts` (7 tests — global/brand gateway visibility, provider-gateway-existence validation, branch-specific-beats-brand-wide resolution ordering, missing-required-credential rejection, encryption round-trip + masking, `resolveAccount()` restriction/priority logic, PUT-lockdown on `provider`/`credentialValues`/`secretCredentials`); full platform regression clean. **Explicitly not built in this phase** (named, not silently deferred): real gateway SDK integrations (Paymob/Stripe/Fawry/etc. — each is separate, ongoing, provider-specific work), `PaymentProviderMapping` (fee consolidation + finer routing rules), `PaymentGatewayTransaction` (authorize/capture/settle lifecycle), the webhook engine, settlement/reconciliation (no business input exists yet to build real matching logic against), and a generic/custom REST adapter for merchant-defined providers with no platform-shipped code.

19. **Enterprise Payment Platform V1 — Phase 2 (Payment Method Resolution Engine)** (2026-07-21, same day as item 18) — closes the gap Phase 1 left open: `PaymentMethod.reference` alone only ever encoded one fixed provider, no priority, no branch override, no automatic fallback. New `PaymentProviderMapping` model (many-to-many `PaymentMethod` ↔ `PaymentProvider`, ranked by `priority`, Global/Brand/Branch scoped like every other layer in this hierarchy) is an **additive, optional** richer layer — a method with no mapping configured still resolves through its own Phase-1 `reference` pointer unchanged, nothing about existing behavior broke. New `payment-method-resolution.service.js` — a real Strategy Pattern registry keyed by `PaymentMethod.type` (`CashRegister`/`PaymentProvider`), each resolver independently swappable, a third `type` value would add one registry entry, never a rewrite. `resolvePaymentProvider()` walks ranked candidates (mapping if configured, else the Phase-1 fallback) and automatically skips any provider that's inactive/archived/channel-mismatched or has no eligible `MerchantAccount`, falling through to the next-ranked candidate — the real automatic-failover the mission asked for, not a placeholder. New endpoint `GET /finance/payment-methods/:id/resolve?channel=&cashRegister=`. **Explicitly, honestly out of scope**: `PaymentMethod.type` only has two real values (`CashRegister`/`PaymentProvider`) — Wallet/GiftCard/Loyalty/Corporate-Billing resolution targets are NOT given fake resolvers, because no real ledger backs any of them anywhere in this codebase (confirmed during the original Phase 0 audit); inventing a resolver for a system that doesn't exist would be exactly the placeholder pattern this phase's own instructions forbid. New `tests/integration/payment-method-resolution.test.ts` (6 tests — cash caller-context-vs-default, no-mapping backward-compat, ranked-mapping automatic failover past a disabled provider, channel-restriction filtering, fails-closed on no eligible account, fails-closed on an unknown method). Full regression clean.

Per-module docs not yet written for most modules — build incrementally, matching the template, rather than inventing a new format per module.

## Foundation review (2026-07-11) — status

A full architectural review of the infrastructure layer (bootstrap, middlewares, `utils/BaseService.js`/`BaseController.js`, DB connection, RBAC, multi-tenancy, settings) was completed and the approved refactoring plan was implemented. Verified end-to-end: full server boot against a real MongoDB instance + smoke test of every mounted route group (all returned correct 401s, zero crashes).

**Resolved:**
1. ~~Broken `utils/AdvancedService.js` import (nonexistent file, used by ~82 files)~~ — fixed, all services import `utils/BaseService.js`.
2. ~~Request context contract bug~~ — `authenticate.js`/`.ts` now sets `req.user.brandId` / `req.user.branchId` / `req.user.userId` explicitly. Previously these were read everywhere (`BaseController`, most custom controllers) but never actually set, meaning `brandScoped` queries silently ignored the brand filter — **every list/read endpoint leaked data across all brands**. This was the most severe finding; it's fixed at the single source (`middlewares/authenticate.js`).
3. ~~RBAC not enforced~~ — `authorize(resource, action)` is now wired into every live router (~38 routers, ~350 call sites), in the order `authenticateToken → authorize → checkModuleEnabled → validate → controller`. Also fixed 3 routers (`brand`, `branch`, `branch-settings`) whose `authorize()` calls existed but used a broken single-argument signature (`authorize("brand:create")`) that would never match any permission — they were effectively locking out every user. `RESOURCE_ENUM` in `modules/iam/role/role.model.js` extended (additive only) to cover CRM, loyalty, seating, audit-log, brand-settings, account-balances, ledgers.
4. ~~No feature-toggle enforcement~~ — new `middlewares/checkModuleEnabled.js`, wired into every router group that maps to a `BrandSettings.modules.*` key. Fail-open by design (no BrandSettings doc, or module key absent → allowed) so no existing brand is affected until they explicitly configure settings. `organization/brand-settings` router (previously built but never mounted) is now live at `/organization/brand-settings`.
5. ~~No `branchScoped` option~~ — added to `BaseService` (opt-in, default `false`, fully backward compatible) and threaded through `BaseController` (`create/getAll/getOne/update/softDelete/restore/hardDelete/bulk*/count`). Also fixed two latent `BaseController` contract bugs found along the way: `hardDelete` never brand-scoped its delete (cross-tenant delete-by-ID was possible), and `bulkHardDelete` called `service.bulkHardDelete(ids)` with a raw array instead of the expected `{ids, brandId, branchId}` object (silently broken).
6. ~~DB connection race + unbounded retry~~ — `connectDB()` is now `await`ed before the app starts listening; retry uses capped exponential backoff (2s → 30s cap) instead of a flat 5s forever-loop; removed dead Mongoose 5 options.
7. ~~JS/TS duplicate infra files~~ — `utils/BaseService.ts`, `utils/pagination.ts`, `utils/joiFactory.ts` were unused, incomplete/incompatible reimplementations (confirmed zero consumers). Deleted rather than merged: TypeScript's `NodeNext` resolution treats a `.js` specifier inside a `.ts` file of the same basename as self-referential, so a thin re-export wrapper is not possible here without renaming — deletion was the safe option. `checkSubscription.ts` was left as-is (it's already the sole implementation; the `.js` sibling is fully commented out).

**Still open:**
8. Order → Invoice → Accounting → Inventory is not connected end-to-end — no code creates JournalEntry postings or deducts stock from invoices/orders yet.
9. ~~JS↔TS migration strategy undecided~~ — **decided 2026-07-11**: TypeScript is now the project's official language. Existing `modules/**` stay JS until each module comes up for rebuild in `IMPLEMENTATION_PLAN.md`'s order; every new module, and every rebuilt module, is written entirely in TypeScript (no mixed JS/TS within one module). Full detail: `server/BACKEND_FOUNDATION.md` §5. TypeScript itself was downgraded from `7.0.2` to `5.9.3` (stable) for tooling compatibility — see the same section for why. ESLint + Prettier are now configured (`npm run lint`/`typecheck`/`format`); testing infrastructure (Jest/etc.) is intentionally postponed to a later phase, not part of this change.
   - **Exception (2026-07-15, owner's explicit request):** `server/modules/sales/order/order.service.ts` and `server/modules/sales/order-settings/order-settings.model.ts` — both originally built under the TS-going-forward policy — were converted back to plain JavaScript (`order.service.js`, `order-settings.model.js`) at the owner's explicit, repeated instruction. Treat `sales/order` and `sales/order-settings` as a deliberate carve-out from the TypeScript policy, not an inconsistency to "fix" by converting them back — do not reconvert these (or any other file) between `.ts`/`.js` without an explicit instruction to do so in that direction.
   - **Second exception (2026-07-19, owner's explicit, informed request — made after being shown the conflict with this documented policy and confirming anyway):** `server/modules/sales/invoice/invoice.service.ts` — pre-existing since 2026-07-12, unrelated to the order/order-settings carve-out above — was likewise converted to plain JavaScript (`invoice.service.js`). Same rule applies: do not reconvert without an equally explicit instruction. `invoice-settings.model.ts`, `invoice-settings.service.ts`, and `payment-provider.model.ts` remain TypeScript — this exception covers `invoice.service` only, not the rest of `sales/invoice`'s or `payments/`'s TS files.
10. Duplicate/legacy trees not yet resolved: `modules/setup/*` (legacy, mostly dead) vs `modules/system-setup/*` (live); `modules/audit-log/*` (live) vs `modules/system/audit-log/*` (abandoned scaffold).

**New pattern to follow for any new module going forward:** every router must chain `authenticateToken, authorize("<ResourceEnumValue>", "<action>"), checkModuleEnabled("<brandSettingsModuleKey>"), validate(schema), controller.method` — copy this order from any router under `modules/sales/`, `modules/accounting/`, etc.
