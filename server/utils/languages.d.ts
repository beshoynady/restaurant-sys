// Type declaration for utils/languages.js — a `.d.ts` file, not `.ts`, for the same reason as
// BaseService.d.ts (see that file's header comment): a `.ts` and `.js` file sharing one basename
// hit a self-referential-import bug under this project's NodeNext module resolution.

export declare const SUPPORTED_LANGUAGES: readonly string[];
export default SUPPORTED_LANGUAGES;
