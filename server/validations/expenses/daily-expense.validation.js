import Joi from "joi";

export const createDaily-expenseSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateDaily-expenseSchema = Joi.object({
  // TODO: define update fields
});