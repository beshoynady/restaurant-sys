import Joi from "joi";

export const createPayment-methodSchema = Joi.object({
  // TODO: define fields based on model
});

export const updatePayment-methodSchema = Joi.object({
  // TODO: define update fields
});