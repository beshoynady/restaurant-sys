import Joi from "joi";

export const createPayment-channelSchema = Joi.object({
  // TODO: define fields based on model
});

export const updatePayment-channelSchema = Joi.object({
  // TODO: define update fields
});