import Joi from "joi";

export const createBank-accountSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateBank-accountSchema = Joi.object({
  // TODO: define update fields
});