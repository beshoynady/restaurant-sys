import Joi from "joi";

export const createCash-transactionSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateCash-transactionSchema = Joi.object({
  // TODO: define update fields
});