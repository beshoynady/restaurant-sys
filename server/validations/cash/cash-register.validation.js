import Joi from "joi";

export const createCash-registerSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateCash-registerSchema = Joi.object({
  // TODO: define update fields
});