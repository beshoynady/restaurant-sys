import Joi from "joi";

export const createCash-transferSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateCash-transferSchema = Joi.object({
  // TODO: define update fields
});