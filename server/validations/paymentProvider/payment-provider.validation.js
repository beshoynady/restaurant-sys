import Joi from "joi";

export const createPayment-providerSchema = Joi.object({
  // TODO: define fields based on model
});

export const updatePayment-providerSchema = Joi.object({
  // TODO: define update fields
});