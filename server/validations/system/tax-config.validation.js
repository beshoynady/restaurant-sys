import Joi from "joi";

export const createTax-configSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateTax-configSchema = Joi.object({
  // TODO: define update fields
});