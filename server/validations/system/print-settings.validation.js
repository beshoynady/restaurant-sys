import Joi from "joi";

export const createPrint-settingsSchema = Joi.object({
  // TODO: define fields based on model
});

export const updatePrint-settingsSchema = Joi.object({
  // TODO: define update fields
});