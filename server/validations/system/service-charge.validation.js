import Joi from "joi";

export const createService-chargeSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateService-chargeSchema = Joi.object({
  // TODO: define update fields
});