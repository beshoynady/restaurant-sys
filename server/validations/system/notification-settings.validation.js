import Joi from "joi";

export const createNotification-settingsSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateNotification-settingsSchema = Joi.object({
  // TODO: define update fields
});