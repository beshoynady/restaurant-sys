import Joi from "joi";

export const createShift-settingsSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateShift-settingsSchema = Joi.object({
  // TODO: define update fields
});