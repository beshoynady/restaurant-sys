import Joi from "joi";

export const createDiscount-settingsSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateDiscount-settingsSchema = Joi.object({
  // TODO: define update fields
});