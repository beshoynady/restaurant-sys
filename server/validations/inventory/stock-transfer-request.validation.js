import Joi from "joi";

export const createStock-transfer-requestSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateStock-transfer-requestSchema = Joi.object({
  // TODO: define update fields
});