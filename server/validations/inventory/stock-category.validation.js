import Joi from "joi";

export const createStock-categorySchema = Joi.object({
  // TODO: define fields based on model
});

export const updateStock-categorySchema = Joi.object({
  // TODO: define update fields
});