import Joi from "joi";

export const createStock-itemSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateStock-itemSchema = Joi.object({
  // TODO: define update fields
});