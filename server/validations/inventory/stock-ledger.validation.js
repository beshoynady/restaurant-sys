import Joi from "joi";

export const createStock-ledgerSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateStock-ledgerSchema = Joi.object({
  // TODO: define update fields
});