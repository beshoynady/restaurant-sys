import Joi from "joi";

export const createInventory-settingsSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateInventory-settingsSchema = Joi.object({
  // TODO: define update fields
});