import Joi from "joi";

export const createInventory-countSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateInventory-countSchema = Joi.object({
  // TODO: define update fields
});