import Joi from "joi";

export const createWarehouse-documentSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateWarehouse-documentSchema = Joi.object({
  // TODO: define update fields
});