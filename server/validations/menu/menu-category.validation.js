import Joi from "joi";

export const createMenu-categorySchema = Joi.object({
  // TODO: define fields based on model
});

export const updateMenu-categorySchema = Joi.object({
  // TODO: define update fields
});