import Joi from "joi";

export const createMenu-settingsSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateMenu-settingsSchema = Joi.object({
  // TODO: define update fields
});