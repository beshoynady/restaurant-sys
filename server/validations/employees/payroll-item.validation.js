import Joi from "joi";

export const createPayroll-itemSchema = Joi.object({
  // TODO: define fields based on model
});

export const updatePayroll-itemSchema = Joi.object({
  // TODO: define update fields
});