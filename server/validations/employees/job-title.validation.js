import Joi from "joi";

export const createJob-titleSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateJob-titleSchema = Joi.object({
  // TODO: define update fields
});