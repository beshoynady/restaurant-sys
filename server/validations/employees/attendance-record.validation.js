import Joi from "joi";

export const createAttendance-recordSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateAttendance-recordSchema = Joi.object({
  // TODO: define update fields
});