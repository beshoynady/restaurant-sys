import Joi from "joi";

export const createEmployee-advanceSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateEmployee-advanceSchema = Joi.object({
  // TODO: define update fields
});