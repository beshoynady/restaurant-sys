import Joi from "joi";

export const createEmployee-financial-transactionSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateEmployee-financial-transactionSchema = Joi.object({
  // TODO: define update fields
});