import Joi from "joi";

export const createEmployee-financialSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateEmployee-financialSchema = Joi.object({
  // TODO: define update fields
});