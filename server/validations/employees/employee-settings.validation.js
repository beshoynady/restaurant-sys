import Joi from "joi";

export const createEmployee-settingsSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateEmployee-settingsSchema = Joi.object({
  // TODO: define update fields
});