import Joi from "joi";

export const createLeave-requestSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateLeave-requestSchema = Joi.object({
  // TODO: define update fields
});