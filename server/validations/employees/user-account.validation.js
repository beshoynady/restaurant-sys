import Joi from "joi";

export const createUser-accountSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateUser-accountSchema = Joi.object({
  // TODO: define update fields
});