import Joi from "joi";

export const createCashier-shiftSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateCashier-shiftSchema = Joi.object({
  // TODO: define update fields
});