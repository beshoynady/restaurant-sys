import Joi from "joi";

export const createDelivery-areaSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateDelivery-areaSchema = Joi.object({
  // TODO: define update fields
});