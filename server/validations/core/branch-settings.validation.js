import Joi from "joi";

export const createBranch-settingsSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateBranch-settingsSchema = Joi.object({
  // TODO: define update fields
});