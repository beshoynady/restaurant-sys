import Joi from "joi";

export const createAsset-depreciationSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateAsset-depreciationSchema = Joi.object({
  // TODO: define update fields
});