import Joi from "joi";

export const createAsset-categorySchema = Joi.object({
  // TODO: define fields based on model
});

export const updateAsset-categorySchema = Joi.object({
  // TODO: define update fields
});