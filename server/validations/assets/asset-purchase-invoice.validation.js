import Joi from "joi";

export const createAsset-purchase-invoiceSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateAsset-purchase-invoiceSchema = Joi.object({
  // TODO: define update fields
});