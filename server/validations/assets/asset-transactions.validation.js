import Joi from "joi";

export const createAsset-transactionsSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateAsset-transactionsSchema = Joi.object({
  // TODO: define update fields
});