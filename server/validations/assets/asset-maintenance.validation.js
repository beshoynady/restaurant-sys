import Joi from "joi";

export const createAsset-maintenanceSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateAsset-maintenanceSchema = Joi.object({
  // TODO: define update fields
});