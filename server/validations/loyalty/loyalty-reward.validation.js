import Joi from "joi";

export const createLoyalty-rewardSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateLoyalty-rewardSchema = Joi.object({
  // TODO: define update fields
});