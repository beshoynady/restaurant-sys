import Joi from "joi";

export const createLoyalty-settingsSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateLoyalty-settingsSchema = Joi.object({
  // TODO: define update fields
});