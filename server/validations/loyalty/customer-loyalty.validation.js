import Joi from "joi";

export const createCustomer-loyaltySchema = Joi.object({
  // TODO: define fields based on model
});

export const updateCustomer-loyaltySchema = Joi.object({
  // TODO: define update fields
});