import Joi from "joi";

export const createLoyalty-transactionSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateLoyalty-transactionSchema = Joi.object({
  // TODO: define update fields
});