import Joi from "joi";

export const createProduction-orderSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateProduction-orderSchema = Joi.object({
  // TODO: define update fields
});