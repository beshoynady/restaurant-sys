import Joi from "joi";

export const createProduction-recipeSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateProduction-recipeSchema = Joi.object({
  // TODO: define update fields
});