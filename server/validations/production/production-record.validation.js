import Joi from "joi";

export const createProduction-recordSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateProduction-recordSchema = Joi.object({
  // TODO: define update fields
});