import Joi from "joi";

export const createOnline-customerSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateOnline-customerSchema = Joi.object({
  // TODO: define update fields
});