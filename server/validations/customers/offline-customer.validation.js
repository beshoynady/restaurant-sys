import Joi from "joi";

export const createOffline-customerSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateOffline-customerSchema = Joi.object({
  // TODO: define update fields
});