import Joi from "joi";

export const createAccounting-settingSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateAccounting-settingSchema = Joi.object({
  // TODO: define update fields
});