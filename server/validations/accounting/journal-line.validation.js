import Joi from "joi";

export const createJournal-lineSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateJournal-lineSchema = Joi.object({
  // TODO: define update fields
});