import Joi from "joi";

export const createAccount-balanceSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateAccount-balanceSchema = Joi.object({
  // TODO: define update fields
});