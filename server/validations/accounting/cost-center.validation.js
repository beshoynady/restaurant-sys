import Joi from "joi";

export const createCost-centerSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateCost-centerSchema = Joi.object({
  // TODO: define update fields
});