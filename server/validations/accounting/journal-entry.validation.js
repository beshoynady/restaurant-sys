import Joi from "joi";

export const createJournal-entrySchema = Joi.object({
  // TODO: define fields based on model
});

export const updateJournal-entrySchema = Joi.object({
  // TODO: define update fields
});