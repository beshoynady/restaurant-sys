import Joi from "joi";

export const createAccounting-periodSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateAccounting-periodSchema = Joi.object({
  // TODO: define update fields
});