import Joi from "joi";

export const createSupplier-transactionSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateSupplier-transactionSchema = Joi.object({
  // TODO: define update fields
});