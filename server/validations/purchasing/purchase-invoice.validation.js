import Joi from "joi";

export const createPurchase-invoiceSchema = Joi.object({
  // TODO: define fields based on model
});

export const updatePurchase-invoiceSchema = Joi.object({
  // TODO: define update fields
});