import Joi from "joi";

export const createPurchase-returnSchema = Joi.object({
  // TODO: define fields based on model
});

export const updatePurchase-returnSchema = Joi.object({
  // TODO: define update fields
});