import Joi from "joi";

export const createPurchase-settingsSchema = Joi.object({
  // TODO: define fields based on model
});

export const updatePurchase-settingsSchema = Joi.object({
  // TODO: define update fields
});