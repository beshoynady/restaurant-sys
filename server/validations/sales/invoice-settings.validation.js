import Joi from "joi";

export const createInvoice-settingsSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateInvoice-settingsSchema = Joi.object({
  // TODO: define update fields
});