import Joi from "joi";

export const createProduct-reviewSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateProduct-reviewSchema = Joi.object({
  // TODO: define update fields
});