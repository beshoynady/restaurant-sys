import Joi from "joi";

export const createOrder-settingsSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateOrder-settingsSchema = Joi.object({
  // TODO: define update fields
});