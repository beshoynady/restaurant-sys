import Joi from "joi";

export const createSales-returnSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateSales-returnSchema = Joi.object({
  // TODO: define update fields
});