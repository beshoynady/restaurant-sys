import Joi from "joi";

export const createSales-return-settingsSchema = Joi.object({
  // TODO: define fields based on model
});

export const updateSales-return-settingsSchema = Joi.object({
  // TODO: define update fields
});