import Joi from "joi";

export const createPreparation-ticketSchema = Joi.object({
  // TODO: define fields based on model
});

export const updatePreparation-ticketSchema = Joi.object({
  // TODO: define update fields
});