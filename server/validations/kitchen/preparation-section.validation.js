import Joi from "joi";

export const createPreparation-sectionSchema = Joi.object({
  // TODO: define fields based on model
});

export const updatePreparation-sectionSchema = Joi.object({
  // TODO: define update fields
});