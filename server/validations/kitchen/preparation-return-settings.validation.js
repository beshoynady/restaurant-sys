import Joi from "joi";

export const createPreparation-return-settingsSchema = Joi.object({
  // TODO: define fields based on model
});

export const updatePreparation-return-settingsSchema = Joi.object({
  // TODO: define update fields
});