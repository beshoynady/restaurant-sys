import Joi from "joi";

export const createPreparation-ticket-settingsSchema = Joi.object({
  // TODO: define fields based on model
});

export const updatePreparation-ticket-settingsSchema = Joi.object({
  // TODO: define update fields
});