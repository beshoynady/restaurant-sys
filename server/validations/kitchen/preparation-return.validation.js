import Joi from "joi";

export const createPreparation-returnSchema = Joi.object({
  // TODO: define fields based on model
});

export const updatePreparation-returnSchema = Joi.object({
  // TODO: define update fields
});